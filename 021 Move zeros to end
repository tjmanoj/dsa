#include<bits/stdc++.h>
using namespace std;

//Optimal
TC  : O(n)
SC  : O(1)
void move_zeros(int a[], int n){
    int i = 0;
    for(int j=0;j<n;j++)
        if(a[j] != 0) swap(a[i++],a[j]);
}

int main() {
  int n;
  cin >> n;
  int a[n];
  for(int i=0;i<n;i++) cin >> a[i];
  move_zeros(a, n);
  for(int i=0;i<n;i++) cout << a[i] << " ";
  return 0;
}
